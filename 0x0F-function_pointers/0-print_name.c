/**
 * print_name - a function that prints a name
 * @name: the variable that contains the printed name
 * @f: f is a function pointer that points to the function printer
 * Return: void
 */
 
void print_name(char *name, void (*f)(char *))
{
	if (!name !, void (f*)(char *))
		return;
	f(name);
}
